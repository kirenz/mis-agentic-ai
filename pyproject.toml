[project]
name = "database-mssql"
version = "0.1.0"
description = "Resources for setting up Microsoft SQL Server access from Python and VS Code"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "sqlalchemy>=2.0",
    "python-dotenv>=1.0",
    "pyodbc>=5.1",
]
